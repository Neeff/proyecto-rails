<div class="container">
<%= form_with(model: nafe, local: true) do |form| %>
  <% if nafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nafe.errors.count, "error") %> prohibited this nafe from being saved:</h2>

      <ul>
      <% nafe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :modelo %>
    <%= form.text_field :modelo %>
  </div>

  <div class="field">
    <%= form.label :fabricante %>
    <%= form.text_field :fabricante %>
  </div>

  <div class="actions">
    <%= form.submit 'Agregar | Modificar', :class => 'btn btn-success'%>
  </div>
<% end %>
</div>